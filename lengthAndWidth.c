#include <stdio.h>

int main() {
    int length = 10, width = 5;  // You can change these values
    int perimeter;

    perimeter = 2 * (length + width);

    printf("Length = %d, Width = %d\n", length, width);
    printf("Perimeter = %d\n", perimeter);

    return 0;
}
