#include <stdio.h>

int main() {
    float base = 10.0, height = 5.0;  // You can change these values
    float area;

    area = 0.5 * base * height;

    printf("Base = %.2f, Height = %.2f\n", base, height);
    printf("Area of the triangle = %.2f\n", area);

    return 0;
}
