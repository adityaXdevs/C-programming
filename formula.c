#include <stdio.h>

int main() {
    float celsius = 25.0; // You can change this value
    float fahrenheit;

    fahrenheit = (celsius * 9 / 5) + 32;

    printf("Celsius: %.2f\n", celsius);
    printf("Fahrenheit: %.2f\n", fahrenheit);

    return 0;
}
