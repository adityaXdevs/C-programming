#include <stdio.h>

int main() {
    int age = 20;  // change the age here

    if (age >= 18)
        printf("Eligible to vote");
    else
        printf("Not eligible to vote");

    return 0;
}
